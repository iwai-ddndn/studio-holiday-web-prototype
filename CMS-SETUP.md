# microCMS セットアップ手順

事例（works）と、左端のニュースレター電光掲示板をmicroCMSから管理できるようにしてあります。

事例（works）1件が、サイト内の次の4か所に同時に反映されます。

| 表示場所 | 内容 |
| --- | --- |
| FVの壁のステッカー | 画像・hover時の`#タグ`・クリック時のドロワー（概要まで） |
| TOPの WORKs 一覧（`#works`） | カード（画像・ジャンル・タイトル・クライアント） |
| 事例ページ `works/<slug>/` | 概要 + **`body` の記事本文**（カード／ドロワーからの遷移先） |
| フッターの WORKs 一覧（全ページ） | タイトル（先頭6件・各事例ページへリンク） |

- **未設定でも動きます**: `cms-config.js` が空の間は、`site.js` 内の
  フォールバックデータ（`WORKS_FALLBACK`）で表示されます。
- CMSの取得に失敗した場合（障害・キー間違い・画像リンク切れ等）も、自動的に
  フォールバックへ切り替わります（コンソールに warn が出ます）。

## 1. サービスを作る

1. https://microcms.io/ でアカウント作成 → サービスを新規作成
2. サービスID（`xxxx.microcms.io` の `xxxx`）を控える

## 2. API を2つ作る

### works（事例 → ステッカー・カード一覧・事例ページ）

- API名: `事例` / エンドポイント: **works** / 型: **リスト形式**

| フィールドID | 表示名 | 種類 | 必須 |
| --- | --- | --- | --- |
| `title` | アウトプット名 | テキストフィールド | ✔ |
| `kind` | ジャンル | テキストフィールド | ✔ |
| `client` | クライアント名 | テキストフィールド | – |
| `description` | 概要 | テキストエリア | ✔ |
| `body` | 詳細記事本文 | リッチエディタ | – |
| `credit` | クレジット | テキストエリア | – |
| `slug` | URLスラッグ | テキストフィールド | –（任意・追加推奨） |
| `sticker` | ステッカー画像 | 画像 | –（任意にすること） |

- **`body`（リッチエディタ）が事例ページの記事本文です。** 見出し・段落・画像をそのまま入稿すれば、
  `works/<slug>/` に記事として表示されます（未入力の場合は「入稿待ち」の注記が出ます）
- `slug` は **半角英数とハイフン**で書きます（例: `holiday-cola`）。事例ページのURLになるため、
  追加しておくことを推奨します（未入力時の決まり方は下の「事例ページのURL」を参照）
- `credit` は **「役割: 名前」を1行ずつ** 書きます。本文の一番下に一覧で表示されます（`:` は全角でも可）

  ```
  Client: 株式会社◯◯
  Creative Direction: 山田太郎
  Art Direction / Design: 鈴木花子
  ```

- `sticker` は **背景を切り抜いた透過PNG** を推奨（白背景JPGでも自動で白を抜きますが精度は落ちます）
- **`sticker` は必須にしないでください。** 画像が未添付の場合、フロント側がタイトルと
  リポジトリ内のローカル素材を突き合わせて自動で画像を紐付けます（メディアアップロードAPIが
  使えないプランでもテキストだけCMS管理できるようにするための仕組み）
- `client` / `kind` / `title` がそのまま hover時の `#タグ` になります
- 画像の当てが無い事例（`sticker` 未添付 かつ ローカル素材とも紐付かない）は、
  FVの壁には出ませんが、カード一覧・事例ページには出ます（カードはプレースホルダー表示）
- 事例を新規追加すると、TOPのWORKs一覧に自動で並びます（初期表示8件 + 「すべての事例を見る」）。
  並び順はmicroCMSの一覧の並び順そのままです

#### 本文の仮原稿

`body` が未入稿の事例のうち、`data/works-sample/<slug>.html` がある事例は、その仮原稿を
レイアウト確認用に表示します（画面上に「仮原稿です」の注記が出ます）。現在は
`holiday-cola`（休日COLA）のみ。**そのHTMLをリッチエディタに貼り、画像をCMS側に
アップし直せばそのまま入稿できます**（`body` が入った時点で仮原稿より優先されます）。

#### 事例ページのURL

恒久URLは `works/<slug>/`（例: `works/sushiro/`）。事例ごとのHTMLを静的生成しています。
ページ内のリンクはすべて自動生成されるので、手でURLを書く場面は基本ありません。

- **slugの決まり方**: microCMSの `slug` フィールド → 無ければローカルフォールバック
  （`site.js` の `WORKS_FALLBACK`）とのタイトル一致で得た `slug` → それも無ければコンテンツID
- microCMSに **`slug`（テキストフィールド・任意・半角英数とハイフン）の追加を推奨**します。
  入れておくとURLがコンテンツIDに依存しなくなります
- 事例ページは `scripts/build_works.py` を実行すると `works/<slug>/index.html` に再生成されます。
  GitHub Actions（`.github/workflows/works-sync.yml`）でも日次で自動再生成されます
- 旧URL `work.html?id=…` は `works/<slug>/` へ自動リダイレクトされます（ブックマーク等は
  そのまま使えます。フォールバックで解決できない新規事例は従来どおり `work.html` がJS描画します）

### newsletter（廃止 → Substack自動連携に変更）

電光掲示板は **microCMSではなくSubstackのRSSから自動取得** します。手動更新は不要です。
microCMSに `newsletter` API を作っていた場合は削除して構いません。

仕組み:
1. GitHub Actions（`.github/workflows/newsletter-sync.yml`）が毎日 JST 6:00 に
   `https://substack.studioholiday.jp/feed` を取得し、`data/newsletter.json` を更新してコミット
   （Actionsタブから手動実行も可能）
2. サイトは `data/newsletter.json` を読み込み、タイトルを電光掲示板に表示。
   **タイトルクリックで該当記事に遷移** します
3. JSONが読めない場合はフィード直接取得を試し、それも失敗したらHTML内の仮タイトルを表示

## 3. APIキーを作って設定する

1. microCMS管理画面 → 「APIキー」→ **GET のみ許可**のキーを作成
   （デフォルトキーをそのまま使う場合も、権限がGETだけになっているか確認）
2. リポジトリ直下の [cms-config.js](./cms-config.js) に記入:

```js
window.MICROCMS_CONFIG = {
  serviceDomain: 'xxxx',      // サービスID
  apiKey: 'XXXXXXXXXXXX',     // GET専用APIキー
};
```

3. ローカルで開いて、FVのステッカーがCMSの内容に変わればOK

## 補足

- キーはフロントに埋め込まれるため**必ずGET専用**にすること（公開コンテンツの参照のみ）
- 画像はmicroCMSのimgix経由で `?w=1000&fm=png` に変換して読み込みます（転送量対策・透過保持）
- 取得は各エンドポイント `limit=100`・タイムアウト4秒。それを超える件数を使う場合は
  `main.js` の `fetchCMS()` にページネーションを足してください
